#include <iostream>
#include <string>

// Αυτό είναι το "Μυαλό" της Βάσης μας.
// Εδώ θα δηλώσουμε τις βασικές τιμές.

using namespace std;

void SetupEconomy() {
    // --- CHALLENGE 1: ΜΕΤΑΒΛΗΤΕΣ ---
    // Αντικατάστησε τα ??? με τις σωστές τιμές.

    // 1. Τα χρήματα που έχουμε στην αρχή (Ακέραιος)
    // int startingMoney = ???;

    // 2. Η ζωή της βάσης (Δεκαδικός - μπορεί να είναι 100.0 ή 50.5)
    // double baseHealth = ???;

    // 3. Το όνομα του Διοικητή (Κείμενο)
    // string commanderName = "???";


    // --- ΜΗΝ ΠΕΙΡΑΖΕΙΣ ΤΙΠΟΤΑ ΠΙΟ ΚΑΤΩ ---
    // (Εδώ θα έμπαινε κώδικας που στέλνει τα δεδομένα στο παιχνίδι)
    // cout << "Commander: " << commanderName << endl;
    // cout << "Money: " << startingMoney << "$" << endl;
    // cout << "Base Health: " << baseHealth << "%" << endl;
}
